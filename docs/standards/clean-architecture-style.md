# 🏗 Архитектурный Code Style (Clean Architecture)

В нашем проекте применяется строгая **Clean Architecture**. Главная цель — независимость бизнес-логики от деталей реализации: фреймворков, баз данных и внешних интерфейсов.

![Clean Architecture Diagram](docs/images/clean-architecture.png)

## 1. Главное правило зависимостей (Dependency Rule)

**Зависимости в коде всегда направлены строго внутрь.** Внешние слои (Controllers, Infrastructure) могут зависеть от внутренних (Application, Domain). Внутренние слои **ничего не знают** о существовании внешних.

## 2. Структура слоев и правила написания кода

### 🟢 Domain (Core)
Сердце приложения. Содержит бизнес-сущности, доменные компоненты, интерфейсы портов (репозиториев) и низкоуровневые варианты использования (не бизнес-правила).

* **Чистый Kotlin:** В этом слое **строго запрещены** любые зависимости от Spring (кроме `@Inject`/`@Named` по необходимости для DI), R2DBC, Jackson, Swagger или Kafka.
* **Принцип единой ответственности (Rich Model):** Сущность изменяет сама себя. Запрещено использовать анемичные модели (data-классы только с геттерами/сеттерами). Вся логика изменения состояния и валидации инвариантов должна находиться внутри самой сущности.
* **Никаких DTO и Entity БД:** Домен оперирует только собственными типами данных.
* **Интерфейсы репозиториев (Ports):** Объявляются здесь (например, `UserRepository`), но реализация выносится в слой Infrastructure.
* **Доменные компоненты (Components / Domain Services):** Инкапсулируют сложную доменную логику, которая требует работы с БД, но не принадлежит конкретной сущности.
  * *Пример:* `UserComponent`. Слой Application (Use Cases) **не обращается** к `UserRepository` напрямую. Вместо этого он вызывает `UserComponent.createUser()`, который внутри себя может обратиться к `UserRepository.findByEmail()`, чтобы проверить уникальность пользователя, а затем вызвать `UserRepository.save()`. Это гарантирует, что правила домена применяются консистентно во всех сценариях.

### 🟡 Application (Use Cases)
Слой бизнес-правил и сценариев использования (Services / Interactors).

* **Ответственность:** Оркестрирует потоки данных. Получает команды снаружи, вызывает доменные компоненты (`UserComponent`) или методы у самих сущностей и управляет транзакциями (если применимо).
* **Ограничения:** Не содержит SQL-запросов, не работает с HTTP-запросами или JSON.

### 🔵 Infrastructure
Адаптеры для взаимодействия с внешним миром (БД, брокеры сообщений, сторонние API).

* **Реализация портов:** Здесь находятся классы, имплементирующие интерфейсы из слоя Domain/Application (например, `PostgresUserRepository : UserRepository`).
* **Технологии:** Это единственное место, где разрешено использовать R2DBC, `DatabaseClient`, аннотации Spring, клиенты Kafka или WebClient.

### 🟣 Controllers
Адаптеры для входящих запросов.

* **Ответственность:** Прием HTTP/REST запросов (Web Controllers), валидация входящих параметров, вызов Application слоя и возврат HTTP-ответов.
* **Ограничения:** Контроллеры **не содержат** бизнес-логики. Их задача — исключительно маршрутизация и маппинг.

---

## 3. Детали реализации: Правила маппинга данных

Мы строго разделяем модели данных. **Запрещено использовать сущности БД в бизнес-логике.**

Обязательный поток преобразования: `DTO` ➡️ `Domain Model` ➡️ `Entity` (опционально)

1. **Входящие запросы (Controllers ➡️ Domain):**
  * Контроллер принимает `DTO`.
  * Контроллер преобразует `DTO` в чистую `Domain Model`.
  * В слой Application передается только `Domain Model`.
2. **Работа с хранилищем (Domain ➡️ Infrastructure):**
  * Репозиторий получает `Domain Model` из слоя доменных компонентов/Application.
  * Репозиторий преобразует `Domain Model` в `Entity` (или мапит напрямую в SQL/DatabaseClient).
  * При чтении из БД репозиторий мапит данные обратно в `Domain Model` перед возвратом.

> **💡 Best Practice:** Для маппинга используйте `extension functions` в Kotlin (например, `fun UserDto.toDomain(): User`).
> Размещайте их в тех слоях, где происходит конвертация (Controllers или Infrastructure).
> Слой Domain должен оставаться чистым от функций маппинга во внешние форматы.
