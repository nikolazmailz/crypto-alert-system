## Git Flow

В команде в качестве эксперимента на 3–4 спринта используется **классический Git Flow**.

Основные ветки:

* `main` — стабильная production-ветка;
* `develop` — основная ветка разработки;
* `feature/*` — ветки для новых задач;
* `release/*` — ветки подготовки релиза;
* `bugfix/*` — исправления ошибок в рамках разработки или стабилизации релиза;
* `hotfix/*` — срочные исправления production.

Подробные правила работы с ветками описаны в [Git Flow](./git-flow.md).

Причины выбора стратегии, рассмотренные альтернативы, плюсы, минусы и критерии пересмотра описаны в [ADR: Выбор стратегии ветвления Git](./adr/ADR-001-git-branching-strategy.md).


#### Основные правила

- `feature/` - используется для новой функциональности. Создаем из `develop`, мерджим в `develop`
- `bugfix/` - используется для исправления ошибок, которые не являются срочным production hotfix. Создаем из `develop`/`release/*`, мерджим в `develop`/`release/*`
- `hotfix` - используется только для срочных исправлений production. Создаем из `main`, мерджим в `main` и `develop`
- `release` - используется для подготовки конкретной версии к выпуску. Создаем из `develop`, мерджим в `develop` и `main` с тегом


## Code Review процесс

**Минимальные требования к MR:**
- МР разрабочтика в приорете, если нет хотфикса, срочного бага, проблем на проде.
- Оповещние об МР:
  - телеграмм
  - тегнуть 2 разработчиков (Денис, плюс кто последний вносил измение в сервис)
  - Реакция `глазки` - желание посмотреть МР
- Description:
  - Кратко описать бизнес смысл, того, что делает доробтка.
- 2+ approve для `feature` в `release` (не относится к хотфиксам)
- happy path тест (на бизнес правило)


## Code Review

Все изменения в основные ветки проходят через Merge Request и Code Review.

Code Review нужен для того, чтобы:

* проверять корректность реализации;
* снижать риск регрессий;
* поддерживать единый стиль разработки;
* делиться знаниями внутри команды;
* не допускать неподготовленные изменения в релиз.

Минимальные правила:

* MR должен содержать ссылку на задачу;
* MR должен содержать краткое описание бизнес-смысла изменения;
* перед merge должен быть успешный build;
* для новой или изменённой бизнес-логики должен быть проверен минимум happy path;
* все замечания reviewer должны быть обработаны или явно обсуждены;
* merge выполняется только после необходимого количества approve.

Подробные правила подготовки MR, назначения reviewers, количества approve и обработки замечаний описаны в [Code Review Process](./code-review.md).
